{
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/app dev",
    "precommit": "pretty-quick --staged"
  },
  "nohoist": [
    "**/react-native",
    "**/react-native/**"
  ],
  "devDependencies": {
    "husky": "^0.14.3",
    "prettier": "^1.14.2",
    "pretty-quick": "^1.6.0"
  }
}
